cmake_minimum_required(VERSION 2.8)

PROJECT(HIVE)

message(HIVE build)

# copy folders

SET(NODE_EDITOR_PATH ${PROJECT_SOURCE_DIR}/tool/SceneNodeEditor)
message(NODEEDITOR ${NODE_EDITOR_PATH})

SET(HIVE_PATH ${PROJECT_SOURCE_DIR}/app)
message(HIVE ${HIVE_PATH})

file(COPY ${NODE_EDITOR_PATH} DESTINATION ${EXECUTABLE_OUTPUT_PATH}/)
file(COPY ${HIVE_PATH} DESTINATION ${EXECUTABLE_OUTPUT_PATH}/HIVE)
