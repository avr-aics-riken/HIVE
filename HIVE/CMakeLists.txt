cmake_minimum_required(VERSION 2.8)

PROJECT(HIVE)

message(HIVE build)

# copy folders

# SceneNodeEditor
SET(NODE_EDITOR_PATH ${PROJECT_SOURCE_DIR}/SceneNodeEditor)
message(NODEEDITOR ${NODE_EDITOR_PATH})

file(COPY ${NODE_EDITOR_PATH} DESTINATION ${EXECUTABLE_OUTPUT_PATH}/)

# HIVE test
SET(HIVE_TEST_PATH ${PROJECT_SOURCE_DIR}/test)
message(HIVE_TEST ${HIVE_TEST_PATH})

file(COPY ${HIVE_TEST_PATH} DESTINATION ${EXECUTABLE_OUTPUT_PATH}/)
file(COPY ${PROJECT_SOURCE_DIR}/../third_party/dkjson.lua DESTINATION ${EXECUTABLE_OUTPUT_PATH}/test)


# HIVE
SET(HIVE_PATH ${PROJECT_SOURCE_DIR}/HIVE_UI)

file(COPY ${HIVE_PATH} DESTINATION ${EXECUTABLE_OUTPUT_PATH}/)
file(COPY ${PROJECT_SOURCE_DIR}/../third_party/dkjson.lua DESTINATION ${EXECUTABLE_OUTPUT_PATH}/HIVE_UI)
