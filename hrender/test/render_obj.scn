
print('Render Obj')

local camera = Camera()
camera:SetScreenSize(1024, 1024)
camera:SetFilename('image.jpg')

camera:LookAt(
	0,0,300,
	0,0,0,
	0,1,0,
	60
)


local obj = OBJLoader()
obj:Load('bunny.obj')

local model = PolygonModel()
model:Create(obj:MeshData())

render {camera, model}

